<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/cameraFragment">

    <action
        android:id="@+id/action_mainActivity_to_cameraFragment"
        app:destination="@id/cameraFragment" />

    <activity
        android:id="@+id/mainActivity"
        android:name="com.zanoapp.applediseaseindentificator.MainActivity"
        android:label="MainActivity">
    </activity>

    <fragment
        android:id="@+id/cameraFragment"
        android:name="com.zanoapp.applediseaseindentificator.uiController.camera.CameraFragment"
        android:label="fragment_camera"
        tools:layout="@layout/fragment_camera" >

    </fragment>


</navigation>